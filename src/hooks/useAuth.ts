// Auth state now lives in AuthContext (single subscription).
// This file is kept as a re-export for any future consumers that need the type.
export type { AuthState } from '../contexts/AuthContext'
